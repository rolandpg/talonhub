version: '3.8'
services:
  api:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - '3000:3000'
    command: node server.js
  frontend:
    image: nginx:alpine
    ports:
      - '80:80'
    volumes:
      - ./dist:/usr/share/nginx/html:ro
