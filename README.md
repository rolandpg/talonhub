# TalonHub

Local OpenClaw Agent Collaboration Hub. Provides a shared operations room for Patrick and the agent fleet:
- Real-time multi-agent chat (WebSocket)
- Screen-share + session casting (WebRTC)
- Artifact exchange (file drops, snippets)
- Skills bridge so each agent participates as a first-class node

See `docs/` for PRD extracts and companion specs.
